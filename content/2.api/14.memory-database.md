---
title: Memory Database
description: A volatile, in-memory storage provider for Moonlink.js. Data is lost when the process restarts.
icon: lucide:cpu
---

The `Memory` database provider is a simple, non-persistent storage implementation. It conforms to the database interface but performs no actual storage operations, effectively acting as a "no-op" or ephemeral cache. It is the default if no database is configured.

## Methods

### init(manager, options)
::api-card{name="init(manager, options)" method="init" returns="Promise<void>" anchorId="init"}
#description
Initializes the memory provider (No-op).

#default
::api-schema{name="manager" type="Manager" required}
The Moonlink Manager instance.
::
::api-schema{name="options" type="object"}
Optional configuration.
::
::

### get(key)
::api-card{name="get(key)" method="get" returns="Promise<undefined>" anchorId="get"}
#description
Always returns `undefined` as this provider does not persist data.

#default
::api-schema{name="key" type="string" required}
The key to retrieve.
::
::

### set(key, value)
::api-card{name="set(key, value)" method="set" returns="Promise<void>" anchorId="set"}
#description
No-op. Does not store the value.

#default
::api-schema{name="key" type="string" required}
The key to store.
::
::api-schema{name="value" type="any" required}
The value to store.
::
::

### has(key)
::api-card{name="has(key)" method="has" returns="Promise<boolean>" anchorId="has"}
#description
Always returns `false`.

#default
::api-schema{name="key" type="string" required}
The key to check.
::
::

### remove(key)
::api-card{name="remove(key)" method="remove" returns="Promise<boolean>" anchorId="remove"}
#description
Always returns `false`.

#default
::api-schema{name="key" type="string" required}
The key to remove.
::
::

### keys(pattern)
::api-card{name="keys(pattern)" method="keys" returns="Promise<string[]>" anchorId="keys"}
#description
Always returns an empty array.

#default
::api-schema{name="pattern" type="string"}
Optional pattern.
::
::

### clear()
::api-card{name="clear()" method="clear" returns="Promise<void>" anchorId="clear"}
#description
No-op.
::

### shutdown()
::api-card{name="shutdown()" method="shutdown" returns="Promise<void>" anchorId="shutdown"}
#description
No-op.
::
